/* MijnSerenity 8.20.1 — compacte AIS-aanvaringsradar op Start */
(()=>{
  'use strict';
  if(window.__msDashboardCollisionRadar8201)return;
  window.__msDashboardCollisionRadar8201=true;

  const RANGE_NM=1.5;
  const FETCH_RADIUS_KM=3;
  const POLL_MS=30000;
  const CACHE_KEY='mijnserenity-ais-last-position';
  const state={radar:null,pos:null,own:{sog:null,cog:null,heading:null,accuracy:null,source:''},targets:[],configured:null,online:false,busy:false,error:'',lastStatus:0,lastFetch:0,timer:null};
  const finite=value=>{const n=Number(value);return Number.isFinite(n)?n:null};
  const angle=value=>{const n=finite(value);return n===null?null:((n%360)+360)%360};
  const first=(...values)=>{for(const value of values){const n=finite(value);if(n!==null)return n}return null};

  function ensureStyle(){
    if(document.getElementById('mscr8201Style'))return;
    const style=document.createElement('style');style.id='mscr8201Style';style.textContent=`
      #msMarineGlass .mg-radar.mscr8201{width:clamp(142px,40vw,176px)!important;height:clamp(142px,40vw,176px)!important;right:16px!important;bottom:58px!important;border:1px solid rgba(157,210,231,.52)!important;background:radial-gradient(circle,rgba(12,43,50,.76),rgba(2,11,17,.97) 72%)!important;box-shadow:0 0 0 7px rgba(3,18,31,.42),inset 0 0 28px rgba(69,226,157,.10),0 10px 26px rgba(0,0,0,.32)!important;overflow:visible!important;cursor:pointer!important;touch-action:manipulation}
      #msMarineGlass .mg-radar.mscr8201:before,#msMarineGlass .mg-radar.mscr8201:after{content:none!important;display:none!important}
      #msMarineGlass .mg-radar.mscr8201.is-risk{border-color:#ff6262!important;box-shadow:0 0 0 7px rgba(255,77,77,.12),inset 0 0 30px rgba(255,77,77,.10),0 10px 26px rgba(0,0,0,.35)!important}
      #msMarineGlass .mg-radar.mscr8201.is-watch{border-color:#ffad38!important;box-shadow:0 0 0 7px rgba(255,159,26,.10),inset 0 0 30px rgba(255,159,26,.08),0 10px 26px rgba(0,0,0,.35)!important}
      #msMarineGlass .mg-radar.mscr8201 .mscr-svg{position:absolute;inset:5px;width:calc(100% - 10px);height:calc(100% - 10px);display:block;overflow:visible}
      #msMarineGlass .mg-radar.mscr8201 .mscr-badge{position:absolute!important;left:50%!important;top:8px!important;bottom:auto!important;z-index:3!important;transform:translateX(-50%)!important;padding:3px 7px!important;border-radius:999px!important;background:rgba(2,14,22,.84)!important;border:1px solid rgba(255,255,255,.14)!important;color:#eaf8ff!important;font-size:9px!important;line-height:1.2!important;font-weight:900!important;letter-spacing:.04em!important;white-space:nowrap!important}
      #msMarineGlass .mg-radar.mscr8201 .mscr-badge.ok{color:#7df29a!important;border-color:rgba(100,223,79,.34)!important}#msMarineGlass .mg-radar.mscr8201 .mscr-badge.warn{color:#ffc569!important;border-color:rgba(255,159,26,.38)!important}#msMarineGlass .mg-radar.mscr8201 .mscr-badge.bad{color:#ff8585!important;border-color:rgba(255,77,77,.36)!important}
      #msMarineGlass .mg-radar.mscr8201 .mscr-range{position:absolute!important;left:50%!important;bottom:8px!important;top:auto!important;z-index:3!important;transform:translateX(-50%)!important;color:#f4fbff!important;font-size:10px!important;font-weight:900!important;white-space:nowrap!important;text-shadow:0 2px 8px #000!important}
      #msMarineGlass .mg-radar.mscr8201 .mscr-summary{position:absolute!important;left:50%!important;bottom:-35px!important;top:auto!important;z-index:3!important;transform:translateX(-50%)!important;width:max-content!important;max-width:190px!important;padding:5px 9px!important;border-radius:999px!important;background:rgba(2,13,21,.93)!important;border:1px solid rgba(122,190,219,.24)!important;color:#d8e8ef!important;font-size:9px!important;line-height:1.15!important;font-weight:800!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important;box-shadow:0 5px 16px rgba(0,0,0,.25)!important}
      #msMarineGlass .mg-radar.mscr8201.is-risk .mscr-summary{color:#ffd0d0!important;border-color:rgba(255,77,77,.45)!important}#msMarineGlass .mg-radar.mscr8201.is-watch .mscr-summary{color:#ffe0ae!important;border-color:rgba(255,159,26,.42)!important}
      @media(max-width:390px){#msMarineGlass .mg-radar.mscr8201{width:140px!important;height:140px!important;right:12px!important;bottom:56px!important}#msMarineGlass .mg-radar.mscr8201 .mscr-summary{max-width:165px!important;font-size:8px!important}}
    `;document.head.appendChild(style);
  }

  function textNum(id){const el=document.getElementById(id);if(!el)return null;const m=String(el.textContent||'').replace(',','.').match(/-?\d+(?:\.\d+)?/);return m?finite(m[0]):null}
  function motion(){
    const nav=window.liveNavState||{};
    const speedKmh=first(nav.speedKmh,nav.speedKmH,textNum('mg-speed'));
    return {
      sog:first(nav.sog,nav.speedKnots,nav.speedKts,nav.speed?.knots,speedKmh!==null?speedKmh/1.852:null),
      cog:angle(first(nav.cog,nav.course,nav.courseOverGround,textNum('mg-course'))),
      heading:angle(first(nav.heading,nav.headingDeg,nav.compassHeading,nav.trueHeading)),
      accuracy:first(nav.accuracy,nav.gpsAccuracy)
    };
  }
  function livePosition(){
    const nav=window.liveNavState||{};let lat=first(nav.currentLat,nav.lat,nav.position?.lat,nav.position?.latitude),lon=first(nav.currentLon,nav.lon,nav.lng,nav.position?.lon,nav.position?.lng,nav.position?.longitude),timestamp=Date.now();
    if(lat===null||lon===null){for(const list of [nav.trackPoints,nav.track,nav.history,nav.gpsTrack,nav.points]){if(!Array.isArray(list)||!list.length)continue;const p=list[list.length-1]||{};lat=first(p.lat,p.latitude);lon=first(p.lon,p.lng,p.longitude);timestamp=first(p.time,p.timestamp)||Date.now();if(lat!==null&&lon!==null)break}}
    if(lat===null||lon===null||Date.now()-timestamp>120000)return null;
    return {lat,lon,timestamp,...motion(),source:'Live GPS'};
  }
  function cachedPosition(){try{const p=JSON.parse(localStorage.getItem(CACHE_KEY)||'null');const lat=finite(p?.lat),lon=finite(p?.lon),timestamp=finite(p?.timestamp);if(lat===null||lon===null||timestamp===null||Date.now()-timestamp>5*60*1000)return null;return {lat,lon,timestamp,accuracy:finite(p.accuracy),sog:finite(p.sog),cog:angle(p.cog),heading:angle(p.heading),source:'Recente GPS-cache'}}catch{return null}}
  function savePosition(p){try{localStorage.setItem(CACHE_KEY,JSON.stringify({lat:p.lat,lon:p.lon,timestamp:p.timestamp||Date.now(),accuracy:p.accuracy,sog:p.sog,cog:p.cog,heading:p.heading}))}catch{}}
  function browserPosition(){return new Promise((resolve,reject)=>{if(!navigator.geolocation)return reject(new Error('GPS niet ondersteund'));navigator.geolocation.getCurrentPosition(pos=>{const c=pos.coords||{},m=motion();resolve({lat:Number(c.latitude),lon:Number(c.longitude),timestamp:Number(pos.timestamp)||Date.now(),accuracy:finite(c.accuracy),sog:first(finite(c.speed)!==null?Number(c.speed)*1.943844:null,m.sog),cog:angle(first(c.heading,m.cog)),heading:m.heading,source:'Apparaat-GPS'})},reject,{enableHighAccuracy:true,maximumAge:10000,timeout:10000})})}
  async function acquirePosition(){const live=livePosition();if(live)return live;try{const gps=await browserPosition();savePosition(gps);return gps}catch{return cachedPosition()}}

  function get(obj,path){try{return path.split('.').reduce((value,key)=>value?.[key],obj)}catch{return undefined}}
  function value(obj,paths){for(const path of paths){const v=get(obj,path);if(v!==undefined&&v!==null&&v!=='')return v}return null}
  function normalize(raw,index){if(!raw||typeof raw!=='object')return null;const lat=finite(value(raw,['latitude','lat','position.latitude','position.lat','location.latitude','lastPosition.latitude'])),lon=finite(value(raw,['longitude','lon','lng','position.longitude','position.lon','position.lng','location.longitude','lastPosition.longitude']));if(lat===null||lon===null||Math.abs(lat)>90||Math.abs(lon)>180)return null;const mmsi=String(value(raw,['mmsi','MMSI','vessel.mmsi','ship.mmsi','identifiers.mmsi','id'])||'').replace(/\D/g,'').slice(0,9),name=String(value(raw,['name','vesselName','shipName','vessel.name','ship.name','static.name'])||'').trim(),sog=finite(value(raw,['sog','speedOverGround','speed','position.sog','position.speed','navigation.sog','lastPosition.sog'])),cog=angle(value(raw,['cog','courseOverGround','course','position.cog','position.course','navigation.cog','lastPosition.cog']));return {id:mmsi||`${lat.toFixed(5)}:${lon.toFixed(5)}:${index}`,mmsi,name:name||(mmsi?`MMSI ${mmsi}`:'AIS-doel'),lat,lon,sog:sog!==null&&sog>=0?sog:null,cog}}
  function collect(v,out=[],depth=0){if(depth>7||v==null)return out;if(Array.isArray(v)){v.forEach(x=>collect(x,out,depth+1));return out}if(typeof v==='object'){out.push(v);Object.values(v).forEach(x=>{if(x&&typeof x==='object')collect(x,out,depth+1)})}return out}
  function targetsFrom(payload){const seen=new Set(),out=[];collect(payload).forEach((raw,i)=>{const t=normalize(raw,i);if(!t)return;const key=t.mmsi||`${t.lat.toFixed(5)}:${t.lon.toFixed(5)}`;if(seen.has(key))return;seen.add(key);out.push(t)});return out}

  function relative(own,t){const lat0=own.lat*Math.PI/180;return {x:(t.lon-own.lon)*60*Math.cos(lat0),y:(t.lat-own.lat)*60}}
  function distance(own,t){const r=relative(own,t);return Math.hypot(r.x,r.y)}
  function velocity(sog,cog){const s=finite(sog),c=angle(cog);if(s===null)return null;if(s<.05)return {x:0,y:0};if(c===null)return null;const rad=c*Math.PI/180;return {x:s*Math.sin(rad)/60,y:s*Math.cos(rad)/60}}
  function cpa(own,t){const r=relative(own,t),tv=velocity(t.sog,t.cog),ov=velocity(own.sog,own.cog);if(!tv||!ov)return null;const vx=tv.x-ov.x,vy=tv.y-ov.y,vv=vx*vx+vy*vy;if(vv<1e-9)return {nm:Math.hypot(r.x,r.y),min:null};const min=-(r.x*vx+r.y*vy)/vv;if(min<=0)return {nm:Math.hypot(r.x,r.y),min};return {nm:Math.hypot(r.x+vx*min,r.y+vy*min),min}}
  function classify(t){const distanceM=t.distanceNm*1852;if(!t.cpa){if(distanceM<50)return 'risk';if(distanceM<120)return 'watch';return 'safe'}const cpaM=t.cpa.nm*1852,future=Number.isFinite(t.cpa.min)&&t.cpa.min>0&&t.cpa.min<=30;if((future&&cpaM<50)||distanceM<50)return 'risk';if((future&&cpaM<200)||distanceM<120)return 'watch';return 'safe'}
  function enrich(list,own){const rank={risk:0,watch:1,safe:2};return list.map(t=>{const x={...t,distanceNm:distance(own,t),cpa:cpa(own,t)};x.level=classify(x);return x}).filter(t=>t.distanceNm<=RANGE_NM*1.25).sort((a,b)=>rank[a.level]-rank[b.level]||a.distanceNm-b.distanceNm).slice(0,20)}
  function color(level){return level==='risk'?'#ff4d4d':level==='watch'?'#ff9f1a':'#65ea72'}
  function point(t){const r=relative(state.pos,t),radius=80;return {x:100+r.x/RANGE_NM*radius,y:100-r.y/RANGE_NM*radius}}
  function targetSvg(t){const p=point(t);if(Math.hypot(p.x-100,p.y-100)>84)return '';let vx=p.x,vy=p.y;if(Number.isFinite(t.sog)&&Number.isFinite(t.cog)){const d=t.sog*.1,rad=t.cog*Math.PI/180;vx+=d*Math.sin(rad)/RANGE_NM*80;vy-=d*Math.cos(rad)/RANGE_NM*80}const c=color(t.level);return `<line x1="${p.x.toFixed(1)}" y1="${p.y.toFixed(1)}" x2="${vx.toFixed(1)}" y2="${vy.toFixed(1)}" stroke="${c}" stroke-width="2.2" stroke-linecap="round"/><circle cx="${p.x.toFixed(1)}" cy="${p.y.toFixed(1)}" r="6" fill="${c}" opacity=".20"/><circle cx="${p.x.toFixed(1)}" cy="${p.y.toFixed(1)}" r="3.6" fill="${c}" stroke="#fff" stroke-width="1.3"/>`}
  function radarSvg(){const rot=angle(state.own.heading??state.own.cog)??0,visible=state.targets.filter(t=>t.distanceNm<=RANGE_NM).slice(0,12);return `<svg class="mscr-svg" viewBox="0 0 200 200" aria-hidden="true"><circle cx="100" cy="100" r="84" fill="rgba(1,12,18,.44)" stroke="rgba(170,216,233,.56)" stroke-width="1"/><g fill="none" stroke="rgba(94,225,165,.22)" stroke-width="1"><circle cx="100" cy="100" r="27"/><circle cx="100" cy="100" r="53"/><circle cx="100" cy="100" r="80"/><line x1="20" y1="100" x2="180" y2="100"/><line x1="100" y1="20" x2="100" y2="180"/></g>${visible.map(targetSvg).join('')}<g transform="rotate(${rot.toFixed(1)} 100 100)"><path d="M100 84 L108 112 L100 108 L92 112 Z" fill="#071720" stroke="#f7fbff" stroke-width="2"/><line x1="100" y1="84" x2="100" y2="69" stroke="#7af19a" stroke-width="2" stroke-linecap="round"/></g></svg>`}
  function fmtCpa(t){if(!Number.isFinite(t?.cpa?.nm))return '';const m=t.cpa.nm*1852;return `CPA ${m<1000?Math.round(m)+' m':t.cpa.nm.toFixed(2)+' NM'}`}
  function fmtTcpa(t){return Number.isFinite(t?.cpa?.min)&&t.cpa.min>0?`${Math.round(t.cpa.min)} min`:''}

  function mount(){ensureStyle();const radar=document.querySelector('#msMarineGlass .mg-radar');if(!radar)return null;if(state.radar!==radar){state.radar=radar;radar.classList.add('mscr8201');radar.setAttribute('role','button');radar.setAttribute('tabindex','0');const open=event=>{event?.preventDefault?.();event?.stopPropagation?.();if(typeof window.captainNavigate==='function')window.captainNavigate('ais');else window.ms708GoToPage?.('ais',true)};radar.onclick=open;radar.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){open(e)}}}return radar}
  function render(){const radar=mount();if(!radar)return;const visible=state.targets.filter(t=>t.distanceNm<=RANGE_NM),worst=visible[0],risk=visible.some(t=>t.level==='risk'),watch=!risk&&visible.some(t=>t.level==='watch');radar.classList.toggle('is-risk',risk);radar.classList.toggle('is-watch',watch);let badgeClass='bad',badge='AIS –',summary='AIS offline';if(state.busy){badgeClass='warn';badge='AIS…';summary='AIS laden…'}else if(state.configured===false){badgeClass='warn';badge='AIS !';summary='AIS instellen'}else if(state.online){badgeClass='ok';badge=`AIS ${visible.length}`;if(!visible.length)summary='Geen doelen binnen 1,5 NM';else if(risk||watch){const bits=[risk?'Risico':'Opletten',fmtCpa(worst),fmtTcpa(worst)].filter(Boolean);summary=bits.join(' · ')}else summary=`${visible.length} doel${visible.length===1?'':'en'} · dichtst ${visible[0].distanceNm.toFixed(2)} NM`}else if(!state.pos)summary='GPS nodig';else if(state.error)summary='AIS niet beschikbaar';radar.innerHTML=`<span class="mscr-badge ${badgeClass}">${badge}</span>${radarSvg()}<span class="mscr-range">1.5 NM</span><span class="mscr-summary">${summary}</span>`;radar.setAttribute('aria-label',`Aanvaringsradar. ${summary}. Tik voor details.`);window.MIJSERENITY_DASHBOARD_AIS={online:state.online,configured:state.configured,count:visible.length,risk,watch,lastFetch:state.lastFetch};window.dispatchEvent(new CustomEvent('mijnserenity:dashboard-ais-update',{detail:window.MIJSERENITY_DASHBOARD_AIS}))}

  async function status(){if(state.configured!==null&&Date.now()-state.lastStatus<5*60*1000)return state.configured;const response=await fetch('/.netlify/functions/ais?mode=status',{cache:'no-store'});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data?.error?.message||`AIS-status ${response.status}`);state.configured=Boolean(data.configured);state.lastStatus=Date.now();return state.configured}
  async function fetchTargets(){const q=new URLSearchParams({mode:'nearby',lat:state.pos.lat.toFixed(6),lon:state.pos.lon.toFixed(6),radiusKm:String(FETCH_RADIUS_KM),limit:'50'}),response=await fetch(`/.netlify/functions/ais?${q}`,{cache:'no-store'}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data?.error?.message||`AIS ${response.status}`);state.targets=enrich(targetsFrom(data?.data??data),state.pos);state.online=true;state.lastFetch=Date.now();state.error=''}
  async function refresh(){if(state.busy)return;state.busy=true;render();try{const p=await acquirePosition();if(!p)throw new Error('Geen actuele GPS-positie');state.pos={lat:Number(p.lat),lon:Number(p.lon)};const m=motion();state.own={sog:first(p.sog,m.sog),cog:angle(first(p.cog,m.cog)),heading:angle(first(p.heading,m.heading)),accuracy:first(p.accuracy,m.accuracy),source:p.source||'GPS'};savePosition({...p,...state.own,timestamp:p.timestamp||Date.now()});if(await status())await fetchTargets();else{state.online=false;state.targets=[];state.error='AIS-databron niet ingesteld'}}catch(error){state.online=false;state.targets=[];state.error=error?.message||'AIS niet beschikbaar'}finally{state.busy=false;render()}}
  function dashboardVisible(){const dashboard=document.getElementById('dashboard');return !document.hidden&&dashboard&&!dashboard.classList.contains('hidden')&&Boolean(document.querySelector('#msMarineGlass .mg-radar'))}
  function start(){mount();render();if(dashboardVisible())refresh();clearInterval(state.timer);state.timer=setInterval(()=>{if(dashboardVisible())refresh()},POLL_MS)}
  [0,250,800,1600].forEach(ms=>setTimeout(start,ms));window.addEventListener('mijnserenity:dashboard-ready',start,{passive:true});window.addEventListener('pageshow',()=>{mount();if(dashboardVisible())refresh()},{passive:true});window.addEventListener('online',()=>{if(dashboardVisible())refresh()},{passive:true});window.addEventListener('offline',()=>{state.online=false;state.error='Geen internetverbinding';render()},{passive:true});
})();